export default function ProductsLayout({ children }) {
    return (
        <div>
            <section>
                {/* Shared UI */}
                Products header
            </section>

            {children}
        </div>
    )
}